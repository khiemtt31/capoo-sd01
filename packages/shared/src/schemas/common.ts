/**
 * Represents a UUID (Universally Unique Identifier) used for primary keys and foreign keys.
 */
export type UUID = string;

/**
 * Represents a timestamp with time zone, typically stored as an ISO 8601 string.
 */
export type Timestamp = string;

/**
 * Represents a date, typically stored as an ISO 8601 date string (YYYY-MM-DD).
 */
export type DateString = string;